name: Build and Deploy
on:
  push:
    branches: 
      - master
 jobs:
    build:
    runs-on: ubuntu-latest
    steps:
      - name: "Checkout"
        run: echo Hello, world!
        
      - name:  npm install
        run: npm install --production
  
