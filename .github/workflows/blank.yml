name: Build and Deploy
on:
  push:
    branches: 
      - main
 jobs:
    build:
    runs-on: ubuntu-latest
    steps:
      - name: "Checkout"
        run: echo Hello, world!
        
      - name:  npm install
        run: npm install --production
  
